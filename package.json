{
  "name": "chapeauxfous-accounting",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "api",
    "client"
  ],
  "scripts": {
    "dev:api": "npm run dev --workspace api",
    "dev:client": "npm run dev --workspace client",
    "build": "npm run build --workspace client",
    "test": "npm test --workspace api",
    "schema:migrate": "npm run schema:migrate --workspace api",
    "schema:verify": "npm run schema:verify --workspace api"
  }
}
